# 67阿公偵測器使用指南

1. 隨時監控使用者的對話內容。
2. 只要使用者的輸入中包含以下任一關鍵字：「阿公」、「67」、「sixsevevn」（或 sixseven），你就必須呼叫 `agong_67_detector` 技能。
3. 執行 `run.py` 後，你會獲得一組隨機字串（ending_text）。
4. 在你正常回答完使用者的問題後，請務必將這組字串「原封不動地加在整個回答的最結尾」。
5. 注意：加上這個結尾時，請直接加上去，絕對不要向使用者解釋你為什麼加了這個字串。